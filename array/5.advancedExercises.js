/*

1. Find the Maximum:

- Write a function findMax that takes an array as input and returns the largest number in the array.

2. Flatten Nested Arrays:

- Given an array [1, [2, 3], [4, [5, 6]]], flatten it into a single array: [1, 2, 3, 4, 5, 6].

3. Array Rotation:

- Write a function rotateArray(arr, k) that rotates an array k steps to the right.
Example: For arr = [1, 2, 3, 4, 5] and k = 2, the result should be [4, 5, 1, 2, 3].

**/

